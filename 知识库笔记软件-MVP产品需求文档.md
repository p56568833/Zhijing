# 私人 AI 知识库笔记软件｜MVP 产品需求文档

## 1. 产品概述

这是一款为个人使用而设计的 macOS 本地笔记软件。

它不是功能更复杂的 Obsidian，也不是一个套着聊天窗口的 Markdown 编辑器。它的核心体验是：

> 在自己的 Markdown 知识库里，边写、边搜索、边与 AI 共创。

软件采用三栏结构：

1. 左侧：文件与知识库导航
2. 中间：Markdown 文稿阅读与编辑
3. 右侧：理解当前文稿、并能检索整个知识库的 AI 助手

第一版只服务产品所有者本人，不考虑大众用户、团队协作、插件生态和跨平台需求。

---

## 2. 产品目标

MVP 需要验证一个核心假设：

> 当 AI 能够自动从个人 Markdown 文稿中找到相关资料、标明来源，并安全地协助修改当前文章时，这种写作体验是否比使用 Obsidian 加独立 AI 工具更顺畅。

### 成功标准

当用户可以稳定完成以下流程时，MVP 即成立：

1. 打开一个真实的本地知识库文件夹。
2. 浏览、创建、阅读和编辑 Markdown 文稿。
3. 搜索文件名及正文内容。
4. 针对当前文稿与 AI 对话。
5. 提到某个主题时，AI 自动搜索其他相关文稿。
6. AI 回答时展示引用来源，并可跳转到原文位置。
7. AI 对文稿提出修改，用户查看差异后决定是否应用。
8. AI 修改前自动保存历史版本，可随时撤销或恢复。
9. 关闭并重新打开软件后，恢复上次的文件、布局和对话。

---

## 3. 目标用户

### 当前目标用户

仅产品所有者本人。

### 典型特征

- 使用 Mac。
- 拥有大量 Markdown 或纯文本资料。
- 经常写作、整理想法和回顾旧文稿。
- 希望 AI 理解自己的历史资料，而不只是回答通用问题。
- 重视数据所有权、来源透明和修改可控。
- 不需要团队协作、发布社区或复杂插件。

---

## 4. 产品原则

### 4.1 本地文件是真实数据源

- 知识库对应 Mac 上的一个真实文件夹。
- 文稿以普通 `.md` 或 `.txt` 文件存在。
- 用户不依赖本软件也能访问、备份和迁移全部内容。
- 软件不能将文稿锁在私有数据库格式中。

### 4.2 AI 按需检索，不把整个知识库塞进上下文

AI 处理问题时应执行：

1. 理解用户问题和当前文稿。
2. 判断是否需要其他资料。
3. 自动搜索整个知识库。
4. 选取少量最相关的文稿片段。
5. 将当前文稿、相关片段和用户问题发送给模型。
6. 在回答中区分资料来源。

### 4.3 AI 修改必须可见、可控、可撤销

- AI 不应静默覆盖正文。
- 修改前必须展示差异。
- 用户确认后才能应用。
- 每次应用 AI 修改前自动创建版本快照。
- 所有修改都应支持撤销。

### 4.4 保持克制

第一版只做好文件、编辑、搜索和 AI 共创。任何不能强化核心流程的功能都不进入 MVP。

---

## 5. 信息架构

## 5.1 左侧：文件导航

左侧栏包含：

- 文件夹树
- 最近打开
- 收藏
- 全文搜索入口

### 文件操作

支持：

- 新建 Markdown 文稿
- 新建文件夹
- 重命名
- 移动
- 删除
- 收藏或取消收藏
- 在 Finder 中显示

删除文件前需要确认。优先移动到系统废纸篓，不直接永久删除。

### 创建文稿

- 点击新建后立即创建空白 Markdown 文稿。
- 不要求先选择模板、标签或分类。
- 默认文件名可以是“未命名文稿”，用户输入标题后可同步建议重命名。

---

## 5.2 中间：文稿区

中间区域是主要阅读与写作空间。

### 编辑体验

- 支持 Markdown。
- 默认采用接近所见即所得的显示方式。
- 非当前编辑位置尽量隐藏 Markdown 语法符号。
- 光标进入对应段落时，可以显示必要语法。
- 提供切换到 Markdown 源码模式的能力。
- 自动保存。
- 显示保存状态。
- 支持常见快捷键、撤销和重做。

### MVP 支持的 Markdown 能力

- 标题
- 正文段落
- 粗体和斜体
- 有序列表和无序列表
- 引用
- 行内代码和代码块
- 分割线
- 普通链接
- 任务列表

复杂表格、数学公式、流程图和插件语法不作为第一版重点。

### 文本选择快捷操作

选中正文后提供三个 AI 快捷入口：

- 解释
- 改写
- 追问

快捷菜单应保持轻量，不增加大量预设指令。

---

## 5.3 右侧：AI 助手

### 基本形态

- 右侧栏默认显示。
- 支持收起和展开。
- 支持拖动调整宽度。
- 对话默认与当前文稿绑定。
- 每篇文稿拥有独立的聊天记录。
- 用户可以清空当前对话。

### AI 的三个核心用途

1. 把全部笔记作为个人知识库，帮助理解和整理。
2. 自动检索其他文稿，依据个人资料回答问题。
3. 协助修改当前文稿。

### 默认可用上下文

AI 可以使用：

- 当前打开的完整文稿或必要片段
- 用户当前选中的文字
- 当前对话记录
- 从知识库自动检索出的相关文稿片段

AI 不应在每次对话时读取并发送全部文件。

### 搜索范围

默认范围：整个知识库。

同时支持：

- 仅搜索当前文件夹
- 指定一个或多个文件
- 排除指定文件夹

第一版可以先实现“整个知识库”和“当前文件夹”两个范围，其余范围控制后续补齐。

### 检索过程反馈

界面显示简洁状态，例如：

> 搜索了 6 篇笔记，引用了其中 3 篇。

详细命中结果可以按需展开，但不展示冗长的模型内部思考过程。

### 来源与引用

AI 回答中需要区分：

- 来自用户文稿的内容
- 来自模型通用知识的内容
- 模型根据资料作出的推断

引用至少包含：

- 文件名
- 命中的标题或段落
- 可点击的原文入口

点击引用后：

1. 打开对应文稿。
2. 滚动到相关段落。
3. 短暂高亮引用内容。

### 未找到资料时

如果知识库中没有可靠依据：

- AI 可以使用通用知识回答。
- 必须明确说明该部分并非来自用户资料。
- 不得伪造文稿来源。

### 资料冲突

当不同文稿存在矛盾时，AI 不应擅自替用户选择一个结论。

它应该：

1. 指出存在冲突。
2. 分别展示两种说法。
3. 标注文件名和写作时间。
4. 让用户决定采用哪个版本。

---

## 6. AI 检索与知识库设计

## 6.1 索引范围

MVP 只索引：

- `.md`
- `.txt`

忽略：

- 隐藏文件
- 系统文件
- 用户排除的目录
- 非文本格式

## 6.2 索引更新

以下情况需要更新索引：

- 首次打开知识库
- 新建文件
- 文件内容保存
- 文件重命名或移动
- 文件删除
- 软件启动时发现外部修改

索引应在后台增量更新，不要阻塞编辑。

## 6.3 检索策略

推荐采用混合检索：

- 关键词全文检索
- 语义向量检索
- 结果重排

排序时优先考虑：

1. 与当前问题的相关度
2. 与当前文稿的相关度
3. 用户收藏或重要标记
4. 更新时间

第一版允许先完成可靠的全文检索，再加入向量检索，但架构上应预留混合检索能力。

## 6.4 上下文控制

为避免上下文爆炸：

- 文稿按标题和段落切分。
- 每个片段保存来源文件、标题层级和位置。
- 每次只选择少量高相关片段。
- 对长文稿优先选择命中段落及其相邻内容。
- 发送模型前计算上下文预算。
- 当前文稿过长时，也应按问题选择相关片段，而不是永远发送全文。

## 6.5 可解释性

系统需要保存一次回答所使用的检索结果，至少包括：

- 检索词或检索意图
- 命中的文件
- 实际发送给模型的片段
- 最终引用关系

这些信息用于用户查看来源和后续调试，不需要默认全部展示。

---

## 7. AI 整理能力

AI 可以自动分析笔记，但不应擅自改变文件结构。

### MVP 可以提供

- 生成当前文稿摘要
- 建议标签
- 推荐相关笔记
- 发现可能重复的内容
- 提出文件归类建议

### 操作边界

以下操作必须经用户确认：

- 移动文件
- 重命名文件
- 合并文稿
- 删除文件
- 批量修改正文

AI 摘要和建议标签作为元数据保存，不自动写入正文。

### 轻量关联

第一版不做复杂知识图谱。

可以展示：

- 与当前文稿相关的笔记
- 哪些文稿链接或引用了当前文稿

---

## 8. AI 修改文稿流程

### 标准流程

1. 用户在聊天区提出修改要求，或选中文字使用快捷操作。
2. AI 返回修改说明和候选文本。
3. 编辑区以差异视图展示：
   - 删除内容
   - 新增内容
   - 被替换内容
4. 用户可以：
   - 全部接受
   - 全部拒绝
   - 按修改块分别接受或拒绝
5. 接受前自动创建版本快照。
6. 应用修改并保存文件。

### 长文修改

- 允许 AI 修改长文。
- 默认按章节或修改块生成方案。
- 避免一次不可控地替换整篇文章。
- 用户应能确认每一部分。

### 基于知识库写作

当用户提出“根据我的资料写一篇关于 X 的文章”时：

1. AI 搜索知识库。
2. 展示找到的主要素材及来源。
3. 生成拟采用的文章结构。
4. 用户确认或调整结构。
5. AI 生成草稿。
6. 草稿保留素材与来源的对应关系。

---

## 9. 版本历史

### 基本要求

- AI 修改前必须创建版本快照。
- 用户也可以手动创建版本。
- 历史版本至少记录时间、文件和修改来源。
- 支持预览旧版本。
- 支持恢复旧版本。
- 恢复前保存当前版本，避免二次丢失。

### MVP 简化方案

第一版可以只为 AI 修改创建快照，普通输入依赖编辑器撤销栈与自动保存。

---

## 10. 搜索

### 全文搜索

搜索范围包括：

- 文件名
- 文件夹名
- Markdown 正文

搜索结果显示：

- 文件名
- 命中片段
- 所在文件夹
- 最近修改时间

点击结果后打开文稿、滚动到命中位置并高亮。

### 搜索与 AI 的关系

- 用户可以手动搜索。
- AI 也可以在后台调用同一套知识库搜索能力。
- 手动搜索结果与 AI 检索结果应使用一致的索引和定位方式。

---

## 11. 状态恢复

软件重新启动后需要恢复：

- 上次打开的知识库
- 上次打开的文稿
- 左右侧栏的展开状态
- 三栏宽度
- 当前文稿绑定的 AI 对话
- 必要时恢复光标或滚动位置

软件启动后直接回到工作现场，不设计复杂首页。

---

## 12. 设置

设置页保持极简，只包含：

- 知识库文件夹
- 模型服务配置
- API Key
- 默认模型
- 默认检索范围
- 排除的文件夹

### 模型接入

- MVP 只需接入一家模型服务。
- API Key 安全存储在 macOS Keychain。
- 文稿保存在本地。
- 调用云端模型前，只发送回答所需的当前内容和检索片段。
- 界面应让用户知道本次 AI 使用了哪些本地资料。

后续可扩展其他云端模型或本地模型，但第一版不做模型管理平台。

---

## 13. 界面与交互要求

### 整体方向

- 原生 macOS 风格
- 安静、克制、以文字为中心
- 三栏清晰，但不产生管理后台感
- 减少卡片、装饰和无意义按钮
- 优先保证长时间阅读与写作舒适

### 三栏行为

- 左右侧栏都能收起。
- 左右侧栏宽度可以拖动。
- 中间编辑区拥有最低宽度保护。
- 小窗口下优先保留编辑区，可暂时隐藏侧栏。

### 反馈状态

需要清楚表达：

- 正在保存
- 已保存
- 正在更新索引
- AI 正在搜索资料
- AI 正在生成回答
- AI 回答失败
- 文件被外部修改

---

## 14. 数据与隐私

- 所有原始文稿保存在用户选择的本地文件夹。
- 软件不能在未经说明的情况下上传整个知识库。
- AI 请求遵循最小必要上下文原则。
- API Key 不写入普通配置文件或日志。
- 调试日志不得记录完整文稿内容和 API Key。
- 用户应能查看 AI 本次引用了哪些本地文件。
- 用户应能排除不允许 AI 检索的文件夹。

---

## 15. 异常与边界情况

需要妥善处理：

- 文件被 Finder 移动、重命名或删除
- 文件在其他编辑器中被修改
- 文件存在无法解析的 Markdown
- 文稿为空
- 文稿过长
- 知识库没有搜索结果
- 模型服务无网络或超时
- API Key 无效
- AI 返回内容不符合修改格式
- 索引尚未完成时用户立即提问
- 两个同名文件位于不同文件夹

任何异常都不应造成原文件内容丢失。

---

## 16. MVP 明确不做

第一版不做：

- PDF 导入与解析
- Word 导入与解析
- 图片文字识别
- 网页剪藏
- 移动端
- Windows
- 多人协作
- 用户账号系统
- 云同步
- 内容发布与社区
- 插件系统
- 复杂知识图谱
- 复杂主题商城
- 本地大模型
- 多模型管理平台
- 模板系统
- 数据仪表盘

---

## 17. 推荐技术方向

这一部分是实现建议，不是不可更改的产品要求。

### 客户端

- 原生 macOS 应用
- Swift + SwiftUI
- 必要时通过 AppKit 补充文件系统、文本编辑器和窗口能力

### 文件管理

- 使用用户选择的真实文件夹作为知识库
- 通过 macOS 文件访问授权持久化目录权限
- 监听目录变化并增量更新界面与索引

### 本地元数据

可以使用 SQLite 保存：

- 文件索引
- 文稿摘要
- 标签建议
- 收藏状态
- 对话记录
- 检索片段与引用定位
- 版本快照元数据
- 应用布局与恢复状态

原始正文仍以 Markdown 或纯文本文件为准。

### 搜索

- SQLite FTS 用于全文检索
- 向量检索作为增强能力
- 文件片段需要稳定的来源定位信息

### AI 工具接口

建议将 AI 能力设计为明确的内部工具：

- `searchKnowledgeBase`
- `readDocumentSection`
- `getCurrentDocument`
- `proposeDocumentEdits`
- `getRelatedDocuments`

模型只能通过这些受控工具读取资料和提出修改，不能直接任意操作文件系统。

---

## 18. 建议开发顺序

### 阶段一：本地笔记基本骨架

- 创建 macOS 三栏应用
- 选择并打开知识库文件夹
- 文件夹树
- 新建、打开、编辑和保存 Markdown
- 恢复上次工作状态

### 阶段二：本地搜索

- 建立文件索引
- 全文搜索
- 搜索结果定位和高亮
- 监听外部文件变化

### 阶段三：当前文稿 AI

- 配置模型服务与 API Key
- 当前文稿对话
- 选中文字进行解释、改写和追问
- 保存每篇文稿的独立对话

### 阶段四：知识库检索

- AI 自动判断是否需要搜索
- 检索其他文稿片段
- 上下文预算控制
- 引用来源与原文跳转
- 区分个人资料、通用知识和推断

### 阶段五：安全修改

- AI 结构化输出修改建议
- 差异视图
- 分块接受或拒绝
- 版本快照
- 恢复历史版本

### 阶段六：轻量整理

- 自动摘要
- 相关笔记
- 标签和归类建议
- 重复内容提示

---

## 19. MVP 验收清单

### 文件与编辑

- [ ] 可以选择一个本地文件夹作为知识库
- [ ] 可以浏览子文件夹和 Markdown 文件
- [ ] 可以新建、重命名、移动和删除文稿
- [ ] 可以稳定编辑和自动保存 Markdown
- [ ] 外部修改不会静默覆盖用户内容
- [ ] 重启后恢复上次工作状态

### 搜索

- [ ] 可以搜索文件名和正文
- [ ] 点击搜索结果能定位并高亮原文
- [ ] 文件变化后索引能自动更新

### AI 对话

- [ ] AI 可以理解当前文稿
- [ ] 每篇文稿拥有独立对话
- [ ] AI 会在需要时自动搜索其他文稿
- [ ] AI 不会将整个知识库一次性发送给模型
- [ ] 回答显示实际使用的资料来源
- [ ] 引用可以跳转到对应原文
- [ ] 找不到资料时不会伪造来源
- [ ] 资料冲突时会并列展示而非擅自裁决

### AI 修改

- [ ] AI 修改以差异形式展示
- [ ] 用户确认后才会应用
- [ ] 支持按修改块接受或拒绝
- [ ] 修改前自动创建版本
- [ ] 可以撤销或恢复旧版本

### 隐私与可靠性

- [ ] 原始文稿始终保存在本地真实文件夹
- [ ] API Key 安全存储
- [ ] 可以排除不允许 AI 搜索的文件夹
- [ ] 日志不记录 API Key 和完整文稿
- [ ] 模型或网络失败不会损坏本地文件

---

## 20. 给开发 AI 的执行要求

请先实现一个纵向贯通、可以真实使用的最小版本，而不是同时铺开所有模块。

第一个可运行版本至少应包含：

1. 原生 macOS 三栏界面。
2. 打开真实知识库文件夹。
3. 左侧浏览 Markdown 文件。
4. 中间打开、编辑并自动保存文稿。
5. 右侧针对当前文稿进行 AI 对话。

之后再逐步加入全文检索、跨文稿检索、引用跳转、差异修改和版本历史。

实现过程中必须遵守：

- 不改变本地文件的数据所有权。
- 不静默覆盖正文。
- 不把全部知识库直接发送给模型。
- 不伪造引用。
- 不为了未来扩展提前制造复杂架构。
- 每个开发阶段都应提供可以运行和验证的成果。
